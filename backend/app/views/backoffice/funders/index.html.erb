<%= render "top_buttons", enable_new_record: true, enable_export: true %>

<div class="flex justify-end mb-4 mr-2">
  <%= render "total_records" %>
</div>

<table class="backoffice-table">
  <thead>
    <tr>
      <th>
        <%= sort_link @q, :id, t("activerecord.attributes.funder.id") %>
      </th>
      <th>
        <%= sort_link @q, :recipient_name , t("activerecord.attributes.funder.name") %>
      </th>
      <th>
        <%= sort_link @q, :updated_at, t("activerecord.attributes.funder.updated_at") %>
      </th>
      <th>
        <%= sort_link @q, :created_at, t("activerecord.attributes.funder.created_at") %>
      </th>
      <th>
      </th>
      <th class="opacity-25">
        <%= svg "menu" %>
      </th>
    </tr>
  </thead>
  <tbody>
    <% @resources.each do |resource| %>
      <tr>
        <td><%= resource.id %></td>
        <td><%= resource.name %></td>
        <td><%= I18n.l resource.updated_at.to_date %></td>
        <td><%= I18n.l resource.created_at.to_date %></td>
        <td><%= link_to t("backoffice.actions.show"), backoffice_funder_path(resource), class: "link-button" %></td>
        <td>
          <%= render "menu" do %>
            <%= render "backoffice/base/menu/item", action: :destroy do %>
              <%= link_to t("backoffice.actions.delete"), backoffice_funder_path(resource),
                data: {
                  turbo_method: :delete,
                  turbo_confirm: t("backoffice.messages.delete_confirmation", model: resource_class.model_name.human.downcase)
                } %>
            <% end %>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
