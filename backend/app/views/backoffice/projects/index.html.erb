<%#= render "new_record", text: t("backoffice.messages.create_new_record", model: resource_class.model_name.human), url: new_backoffice_project_path %>

<table class="backoffice-table">
  <thead>
    <tr>
      <th>
        <%= sort_link @q, :id, t("activerecord.attributes.upload.id") %>
      </th>
      <th>
        <%= sort_link @q, :recipient_name , t("activerecord.attributes.recipient.name") %>
      </th>
      <th>
        <%= sort_link @q, :updated_at, t("activerecord.attributes.recipient.updated_at") %>
      </th>
      <th>
        <%= sort_link @q, :created_at, t("activerecord.attributes.recipient.created_at") %>
      </th>
      <th>
      </th>
    </tr>
  </thead>
  <tbody>
    <% @resources.each do |resource| %>
      <tr>
        <td><%= resource.id %></td>
        <td><%= resource.name %></td>
        <td><%= I18n.l resource.updated_at.to_date %></td>
        <td><%= I18n.l resource.created_at.to_date %></td>
        <td><%= link_to t("backoffice.actions.show"), backoffice_project_path(resource), class: "link-button" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
