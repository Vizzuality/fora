<% if can? action&.to_sym, resource_class %>
  <li role="menuitem" tabindex="-1" class="text-sm transition text-green-0 border-[1px] border-green-0/10">
    <div>
      <%= yield if yield.present? %>
    </div>

<% end %>
