<% if can? :create, resource_class %>
  <%= link_to new_resource_url do %>
    <div class="bg-white text-green-0 rounded-full py-2 px-4">
      <span class="text-xl leading-none">⊕</span>
      <span><%= t("backoffice.messages.create_new_record", model: resource_class.model_name.human) %></span>
    </div>
  <% end %>
<% end %>
