<div class="w-full">
  <div class="absolute -top-6 right-0 flex gap-2">
    <% if enable_new_record %><%= render "new_record" %><% end %>
    <% if enable_export %><%= render "export_csv" %><% end %>
  </div>
</div>
