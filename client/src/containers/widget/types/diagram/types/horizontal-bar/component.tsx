import { Widget } from 'types/widget';

const WidgetDiagramHorizontalBar = (widget: Widget) => {
  console.info('WidgetDiagramHorizontalBar', widget);
  return <div>Horizonal Bar</div>;
};

export default WidgetDiagramHorizontalBar;
