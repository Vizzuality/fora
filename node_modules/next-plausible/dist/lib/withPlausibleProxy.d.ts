import { NextConfig } from 'next';
import { NextPlausibleProxyOptions } from './common';
export default function withPlausibleProxy(options?: NextPlausibleProxyOptions): (nextConfig: NextConfig) => NextConfig;
